# iPhone 18

一台 432 毫米长的手机的官网。《爱情公寓》里胡一菲举着它问「这爱疯18怎么用」，这个页面就是它的官方产品页。

**这是一个虚构的概念设计，与 Apple Inc. 无关。**

## 这是什么

不是一个普通的产品落地页，是一次仿冒。整个页面的目标是：在前三秒里让人以为这是苹果官网，然后才反应过来这台手机长得不对劲。所以视觉系统不做任何自由发挥，色值、字号、字距、按钮、导航、法务区块全部照抄苹果在用的真实数值；笑点全部交给参数和不加修饰的渲染，文案一句玩笑都不开。

技术上是 Vite + TypeScript + three.js。一台程序化生成的手机，一支从头到尾不切镜的摄影机，滚动位置直接映射成镜头轨迹。

## 跑起来

```bash
npm install
npm run dev        # http://localhost:5273
npm run build      # 类型检查 + 生产构建
npm run typecheck
```

`node_modules` 之外的依赖只有 `three` 和 `lenis`。

## 设计的主轴

**手机的 432 毫米，就是页面的长度。**

一个 6:1 的物体放进 16:9 的视口，只能是一根牙签。常规做法是缩小居中，那样整页都废了。这里的做法是永远不给看全貌，直到笑点该落地的那一刻：往下滚，就是沿着机身往下走。

九个折页，一台摄影机：

| # | 折页 | 镜头 |
|---|---|---|
| 0 | Hero | 3/4 特写，只看得见上半截，下半身出血 |
| 1 | 432 毫米 | 拉远，第一次完整入画，三条尺寸标注线 |
| 2 | 超高屏幕 | 贴上屏幕表面，沿机身往下飞 260 毫米 |
| 3 | 铝金属 | 绕机身公转，掠射光扫过边缘 |
| 4 | 7.8 毫米 | 切正侧 + 摄影机横滚 90°，厚度变成一条发丝 |
| 5 | 细节之旅 | 从背面镜头一路推到 USB-C，一折跑完整机长度 |
| 6 | 电池 / 简约 | 退回正面全身，机身站在右栏 |
| 7 | 技术规格 | 表格居中，机身安静站在右侧 |
| 8 | 购买 | 回到首屏的裁切，收尾 |

## 代码怎么组织的

```
src/
  main.ts                 启动、能力检测、渲染循环
  style/
    tokens.css            设计令牌（苹果真实取值，逐对核过对比度）
    app.css               图层顺序、折页、文案、规格表
  scene/
    dimensions.ts         产品尺寸表，1 单位 = 1 毫米
    geometry.ts           圆角矩形挤出、程序化机身
    materials.ts          铝金属 / 屏幕 / 镜片
    textures.ts           背面蚀刻 logo
    screen.ts             1440 × 8717 的屏幕贴图（锁屏 + 18 条通知）
    phone.ts              组装整机
    stage.ts              renderer、摄影棚环境光、DPR、降级
    overlay.ts            尺寸标注线，投影自三维世界坐标
  direction/
    keyframes.ts          镜头轨道，一个折页一段
    timeline.ts           关键帧插值 → 摄影机
    scroll.ts             滚动 → 0..1 进度，临界阻尼
  ui/reveal.ts            入场揭示（内容默认可见，只做增强）
```

## 几处不显然的决定

**几何全部程序化，不下载模型。** 机身是一个圆角矩形挤出体，1 个场景单位 = 1 毫米，所以几何参数、镜头framing 和页面上的标注线说的是同一套数字，改尺寸不用回 Blender，也没有任何外部资源会 404。

**ExtrudeGeometry 的 bevel 必须是正数。** 正号给出正确的 iPhone 截面（平面内缩 65.6 毫米，中段最宽 72 毫米）；负号会把它翻成一条面包。这个符号是实测出来的，不是猜的。

**摄影棚环境光是手写的，不是 three 的 RoomEnvironment。** metalness 为 1 时铝金属没有漫反射项，环境贴图就是机身颜色本身。RoomEnvironment 平均太暗，机身渲染出来是灰的，而参考图是近白。自建的光照里有一块很亮的天花板、两块不对称的侧板（负责边缘那道竖直高光）和一块故意压暗的地板（负责背面自上而下的渐变）。

**near/far 是按深度精度选的，不是按裁剪选的。** 最近的关键帧是 52 毫米的镜片微距，最远的是 2.3 米的规格页。near = 1 时深度缓冲分辨不出屏幕与机身之间零点几毫米的高差，屏幕会直接被机身盖住（`duo` 折页曾经整块消失）。

**折页背景、机身、文案是三层的。** 折页底色 z-index 0，canvas z-index 1，文案 z-index 2。所以 `.fold` 本身不能带 z-index，否则底色和文案会被困在同一层，文案会掉到 canvas 后面。深色折页尤其重要：`.fold--dark` 只改文字颜色，底色得靠 `.fold::before` 真正铺上黑。

**水平构图用 truck，不用 target.x。** truck 让摄影机和目标点一起沿摄影机右轴平移，主体在画面里左右移动但距离不变。挪 target.x 是更顺手的写法，但它同时改变了摄影机到主体的距离，会悄悄把构图改掉。

**镜头关键帧锚在 DOM 元素上，不是锚在滚动比例上。** 每把镜头记录它对应哪个 `[data-anchor]`，以及那一刻该元素的中心应落在视口高度的百分之几。改 CSS 高度不会让镜头和文案错位；反过来，如果两把镜头解析出了倒序，`resolveTimeline` 会在开发模式里警告。

## 降级

- **没有 WebGL**：用 `public/poster.jpg` 加同一套 DOM 文案。海报由 `scratch/poster.cjs` 生成。
- **`prefers-reduced-motion: reduce`**：不创建 Lenis，滚动退回原生，机位完全不动，关掉待机漂移。
- **软件光栅化**（SwiftShader / llvmpipe，常见于无头环境和虚拟机）：把各向异性过滤降到 1、DPR 锁 1。1024 × 6432 的屏幕贴图在软件光栅化下做各向异性过滤会让每帧掉到几秒，而且纹理本身会出现摩尔纹。
- **移动端**：`truck` 跟随视口宽高比衰减到 0，并叠加一个按可见高度计算的台座偏移，把机身推离文案所在的那一半。

## 屏幕里的东西

那块屏幕是 1440 × 8717 的 CanvasTexture，按真实 iOS 的 pt 尺寸换算绘制（17 pt 正文 = 62 单位，38 pt 通知图标 = 139 单位）。里面是一整屏锁屏加 18 条通知，一次看完不用滚动，这是「超高屏幕」那一段的全部论据。

时钟字号是按屏幕**宽度**定的，不是按高度：参考图里的时钟占屏宽约 44%，按 6:1 屏幕的高度去缩放会把它变成一块广告牌。

## 验收

```bash
# 逐折页截图（需要 Playwright）
$env:NODE_PATH='<npm 全局 node_modules>'
node scratch/shoot.cjs

# 移动端
$env:SHOOT_W='390'; $env:SHOOT_H='844'; $env:SHOOT_NO_DUMP='1'
node scratch/shoot.cjs

# 重新生成 no-WebGL 海报
node scratch/poster.cjs
```

截图脚本用真实 GPU（ANGLE/D3D11）跑，因为软件光栅化会把各向异性过滤降到 1，屏幕贴图会出现并不存在的摩尔纹。截图时模拟 `prefers-reduced-motion`，这样 Lenis 不介入、`window.scrollTo` 落点精确；动效路径单独用 `SHOOT_MOTION=1` 冒烟验证。

## 目录说明

- `docs/` 是产品外观的唯一基准：`iphone18.png` 那张概念图，以及剧集截图。
- `scratch/` 是草稿箱，不入库。里面有量测脚本、截图脚本和中间产物。

## 已知的取舍

- **参考图自身不一致。** 图上印着 432 × 72 × 7.8 毫米，但画出来的比例约为 4.26:1，也就是 432 × 101 × 12.7 毫米。432 = 6 × 72 是刻意凑的整数，整页的「长到装不下」也依赖 6:1，所以取标注尺寸为准，把图里所有**脸部比例**（内缩宽度、镜头位置、logo 高度、天线带、按键、接口）按占机身的比例换算过来。
- **屏幕四角接近直角。** 量下来参考图的屏幕在第一个暗像素行就占满整宽，没有可见圆角，所以只留了 2 毫米的极小圆角避免走样。
- **构建产物 545 kB（gzip 143 kB）**，基本都是 three.js。这个页面不需要首屏之外的内容，真要进一步压可以按折页做动态导入。
