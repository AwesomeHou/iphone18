# Design

## Visual Theme

**复刻，不是重新诠释。**

这个页面的视觉系统就是苹果自己的视觉系统。银白铝金属的机身、纯黑与 #f5f5f7 交替的折页、极短句加句号收尾的排版、17px 正文、980px 药丸按钮、48px 高的毛玻璃全局导航。所有取值尽量照抄苹果真实在用的数值，不发明新的品牌色。

之所以不做「苹果风但更有个性」的变体：这个项目的笑点全部寄生在「看起来就是苹果」之上。任何一处自由发挥都会削弱前缀，然后笑话就不成立了。

唯一真正自由的地方是三维：一台 432 毫米长的手机在真实光照下的样子。

**Light / dark 判断**：页面的物理场景是一间苹果零售店的展台，白墙、漫射光、产品被打亮。这决定了浅色 `#f5f5f7` 是主语，纯黑折页只在需要让屏幕发光的时候出现（屏幕折页、规格折页）。不是「深色显得酷」，也不是「浅色保险」。

## Color Palette

照抄苹果在用的数值。保真优先于另造品牌色，且这些值经核对本身满足 WCAG AA。

| Token | Value | 用途 | 对比度 |
|---|---|---|---|
| `--bg-light` | `#f5f5f7` | 浅折页底色 | 正文 `#1d1d1f` 于其上 15.6:1 |
| `--bg-dark` | `#000000` | 屏幕折页、规格折页底色 | |
| `--surface` | `#ffffff` | 卡片、规格表 | |
| `--ink` | `#1d1d1f` | 主标题与正文 | |
| `--ink-2` | `#6e6e73` | 次级正文 | 于 `#f5f5f7` 上 4.66:1，达标 |
| `--ink-3` | `#86868b` | 辅助说明 | 于 `#000` 上 5.80:1，达标 |
| `--on-dark` | `#f5f5f7` | 深色折页正文 | 于 `#000` 上 19.3:1 |
| `--accent` | `#0071e3` | 购买按钮、链接 | 白字于其上 4.70:1，达标 |
| `--hairline-dark` | `rgba(255,255,255,.16)` | 深色折页分隔线 | |
| `--hairline-light` | `rgba(0,0,0,.12)` | 浅色折页分隔线 | |

**色彩策略：Restrained。** 中性底加单一强调色（购买蓝），强调色占比远低于 10%。这是苹果的真实策略，而这里的克制本身就是仿冒对象的一部分。

**三维材质色**（与二维色板分开，属于场景）：
- 机身铝金属 `#e3e4e6`，metalness 0.9，roughness 0.34，加清漆层模拟阳极氧化
- 屏幕玻璃 `#050506`，roughness 0.06，加微弱清漆反射
- 摄像头镜片 `#0b0b0d`，roughness 0.12

## Typography

**字体栈（照抄苹果的真实栈，不打包 webfont）：**

```
-apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
"Helvetica Neue", Inter, system-ui, "PingFang SC", "Microsoft YaHei", sans-serif
```

这是本项目唯一一处刻意违背通用「反射黑名单」的地方，理由是身份保真：这个页面的一切都建立在 SF 之上，换字体会立刻毁掉仿冒。授权原因不打包 SF webfont，苹果设备上完美，其他平台退到近似字面。中文由 PingFang SC（macOS/iOS）与微软雅黑（Windows）承接，与 SF 的字重气质一致。

**字阶**（比例均 ≥ 1.25，流体 clamp）：

| Token | 值 | 用途 |
|---|---|---|
| `--fs-display` | `clamp(2.75rem, 1.2rem + 6.4vw, 5.5rem)` | Hero 主标题，上限 88px |
| `--fs-h2` | `clamp(2rem, 1.1rem + 3.6vw, 3.5rem)` | 折页主陈述 |
| `--fs-h3` | `clamp(1.5rem, 1.1rem + 1.6vw, 2rem)` | 规格分组 |
| `--fs-lead` | `clamp(1.25rem, 1.05rem + .8vw, 1.75rem)` | 引导段 |
| `--fs-body` | `1.0625rem` (17px) | 正文，苹果的实际取值 |
| `--fs-small` | `.875rem` | 标注 |
| `--fs-micro` | `.75rem` | 导航、脚注 |

字距：`--fs-display` 用 `-0.02em`，`--fs-h2` 用 `-0.01em`，正文 `-0.006em`。绝不收紧到 `-0.04em` 以下。

正文行高 1.47（苹果取值），正文行宽限制在 65ch。深色底上的浅色字行高加 0.05。

## Layout

- 每个折页一个 `100vh` 的语义单元，部分折页取 `120vh` 以获得节奏变化。全程只用一支摄影机，滚动位置即时间轴位置。
- 折页内的文案与三维主体用 CSS Grid 分栏，窄屏塌成单栏，三维主体退到底层。
- 间距用 8px 基准的流体 `clamp()`，折页上下内边距 `clamp(80px, 12vh, 160px)`。
- 折页之间不重复同一套构图：Hero 是左文右机、揭晓是居中标注图、屏幕折页是满幅沉浸、规格折页是居中的表格。反的就是「每个折页长得一样」。
- 圆角：药丸按钮 `980px`，方框 `18px`，小控件 `12px`。不上 32px 以上的大圆角。

## Motion

- 缓动统一用 `cubic-bezier(0.16, 1, 0.3, 1)`（ease-out-expo 近似）。无回弹、无弹性。
- 三维镜头由单一的滚动进度值驱动，临界阻尼 lerp 跟随，让它比 DOM 略微滞后一拍：那一点滞后就是「贵」的来源。
- 文案揭示是增强而非门禁：默认可见，`IntersectionObserver` 只做位移与透明度动画。绝不把内容藏在等待触发的过渡后面。
- `prefers-reduced-motion: reduce` 时：关掉平滑滚动、关掉镜头刮擦，折页退化为常规文档流，全部内容可见。

## Components

| 组件 | 规格 |
|---|---|
| 全局导航 | 48px 高，`rgba(22,22,23,.8)` 加 `backdrop-filter: blur(20px)`，12px 链接，最大宽度 1024px 居中。保留毛玻璃是因为这是对真实组件的忠实复刻，不是装饰性玻璃卡片 |
| 购买按钮 | 药丸形，`--accent` 底，白字，17px，内边距 `8px 16px` |
| 次级链接 | 17px，`--accent` 文字，语气是「进一步了解 >」 |
| 规格表 | 白底，1px `--hairline-light` 分隔行，左键右值，键名 `--ink-2`，值 `--ink` |
| 尺寸标注 | 1px 线加端点，`--fs-micro` 标注文字，投影自三维世界坐标 |
| 折页陈述 | 居中，`--fs-h2`，下方 `--fs-lead` 引导段，最大宽度 65ch |
